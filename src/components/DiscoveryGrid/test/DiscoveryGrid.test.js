import React from "react";
import DiscoveryGrid from "../DiscoveryGrid";

describe("discoveryGrid", () => {
  it("renders without crashing", () => {
    const wrapper = shallow(<DiscoveryGrid />);
  });
});
